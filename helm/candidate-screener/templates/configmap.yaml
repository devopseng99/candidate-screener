apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "candidate-screener.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "candidate-screener.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
